using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonListener : MonoBehaviour
{
    //private Connection _connection;

    //private void Start()
    //{
    //    _connection = new Connection("https://www.bigscreens.live/socket.io/", "BeatMatchingProject", "game");
    //    _connection.OnConnect(() => { Debug.Log("Connected"); });
    //    _connection.OnDisconnect(() => { Debug.Log("Disconnected"); });
    //    _connection.OnOtherConnect((otherId, type) =>
    //    {
    //        if (type == "admin")
    //            Debug.Log("Admin Connected");
    //    });
    //    _connection.OnOtherDisconnect((otherId, type) =>
    //    {
    //        if (type == "admin")
    //            Debug.Log("Admin Disconnected");
    //    });
    //    _connection.On("button_press", (string senderId, string buttonType) =>
    //    {
    //        Debug.Log("Button pressed: " + buttonType);
    //    });
    //    _connection.Open();
    //}
}